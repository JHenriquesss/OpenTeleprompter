# Rust build artifacts
target/
src-tauri/target/

# Trunk frontend dist
dist/

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Environment
.env
.env.local

# Updater signing keys — NEVER commit the private key.
# Only the public key is embedded (in tauri.conf.json); the private key signs
# release bundles and lives in the GitHub Actions secret TAURI_SIGNING_PRIVATE_KEY.
.updater-keys/

# Runtime database files
*.db
*.db-shm
*.db-wal
openprompter-rs/
