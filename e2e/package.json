{
  "name": "openprompter-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end tests that launch the real built app and click real buttons via tauri-driver. Catches frontend<->backend IPC regressions (e.g. the withGlobalTauri dead-buttons bug) that unit/MockApi tests cannot.",
  "scripts": {
    "test": "wdio run wdio.conf.js"
  },
  "devDependencies": {
    "@wdio/cli": "^9.0.0",
    "@wdio/local-runner": "^9.0.0",
    "@wdio/mocha-framework": "^9.0.0",
    "@wdio/spec-reporter": "^9.0.0"
  }
}
