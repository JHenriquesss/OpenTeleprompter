{
  "identifier": "default",
  "description": "Default capabilities for OpenPrompter RS",
  "windows": ["main", "pip"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:default",
    "updater:default"
  ]
}
