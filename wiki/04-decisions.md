# Decision Log

| Date | Decision | Rationale | Alternatives rejected |
|------|----------|-----------|---------------------|
| 2026-06-06 | `rust-toolchain.toml` over `rustup` manual | Reproducible toolchain across dev + CI; auto-installs wasm32 target + components | README instructions only |
| 2026-06-06 | CI validates backend via `-p openprompter-rs-tauri`, frontend via `trunk build` | Frontend uses `web-sys`/`wasm-bindgen` → only compiles for wasm32; backend is native | `cargo check --workspace` (fails on frontend) |
| 2026-06-06 | Dual scripts (`.sh` + `.ps1`) for validation pipeline | Team uses both Linux/macOS and Windows; no npm/node dependency | Single script, Makefile, justfile |
|------|----------|-----------|---------------------|
| 2026-06-06 | rusqlite (direct) over tauri-plugin-sql | More control, no JS bridge overhead, pure Rust | tauri-plugin-sql |
| 2026-06-06 | Hardcoded AppSettings defaults | Simpler, no config file to ship | defaults.toml shipped with app |
| 2026-06-06 | Frontend-driven file dialogs | No Tauri v2 dialog plugin needed for MVP; HTML file input + Blob download works | rfd crate, tauri-plugin-dialog |
| 2026-06-06 | Leptos CSR over SSR | No server needed, fully offline, simpler deploy | Leptos SSR, Yew, Sycamore |
| 2026-06-06 | RwSignal contexts over global store | Leptos idiomatic, no extra dependency | leptos-use, state crate |
| 2026-06-06 | `Rc<Cell<bool>>` alive flag for animation loop cleanup | Stops `requestAnimationFrame` chain on unmount without leaking | `cancelAnimationFrame` handle, drop Closure |
| 2026-06-06 | `on_cleanup` for keyboard event listener | Removes listener on component unmount, prevents duplicate handlers | `closure.forget()` only (was leaking) |
| 2026-06-06 | Cargo workspace root + `src-tauri/tests/` | Enables `cargo test` from root; tests need `rusqlite` which only src-tauri has | Root-level `tests/` with dev-deps duplication |
| 2026-06-06 | Removed `RequestAnimationFrame` from web-sys features | Feature doesn't exist in web-sys v0.3.63 (tauri's resolved dep). `Window` feature provides `request_animation_frame()` | Patch web-sys to ^0.3.64 (would conflict with tauri's lock) |
| 2026-06-06 | Removed `uuid` + `chrono` from root Cargo.toml | uuid v1 on wasm32 requires `js`/`rng-getrandom` feature; neither used in frontend code. chrono also unused | Adding `js` feature to uuid |
| 2026-06-06 | `index.html` uses `<link data-trunk rel="rust" data-bin="openprompter-rs" />` | Crate has both lib + bin targets; trunk needs explicit target disambiguation | `<script data-bin>` attribute (didn't work) |
| 2026-06-06 | `Trunk.toml` watch.ignore left empty | Non-ASCII Windows paths break `canonicalize()` in trunk's watch file scanner | Glob pattern without canonical paths |
| 2026-06-06 | Pre-install `wasm-bindgen-cli` via `cargo install` | trunk downloads wasm-bindgen CLI via GitHub releases; GitHub 504 timeout under load | Rely on trunk's auto-download |
| 2026-06-06 | Typed wrapper pattern (`bindings/tauri_api.rs`) over raw `invoke()` calls in components | Isolates invoke JSON plumbing from UI code; typed structs provide compiler-checked field access; easy to mock for testing | Inline invoke calls per component |
| 2026-06-06 | `library_refresh_trigger: RwSignal<u32>` + `refresh_library()` for library re-fetch | Coarse-grained but simple: incrementing counter triggers resource re-evaluation. No need for manual fetch callback wiring | Direct resource refetch signal, store-based cache invalidation |
| 2026-06-06 | `save_current()` closure extracted from `on:click` handler | Both Save button (MouseEvent) and "Open in Prompter" button (save-then-navigate) need same save logic. Shared closure avoids duplication | Duplicate save logic, closure taking `impl Into<Event>` |
| 2026-06-06 | CSS variables + `<style>` tag for theming over inline style rewriting | All component styles are hardcoded inline; rewriting them to use CSS variables would touch every file. Phase 5 adds toggle infrastructure + scrollbar styling only | Rewriting all inline styles to CSS variables |
| 2026-06-06 | Theme persisted through existing settings flow (no new backend table/command) | `Theme` enum already in `domain/settings.rs` with `Light`/`Dark`, serialized as string in `AppSettingsData`. Adding a separate theme endpoint would duplicate the settings CRUD | New `set_theme`/`get_theme` Tauri commands |
| 2026-06-06 | Ctrl+S on per-input `on:keydown` (not window-level listener) | Only editor inputs need the shortcut; window-level listener would fire even when sidebar/prompter is focused (could conflict with other Cmd+S behavior) | `window.add_event_listener("keydown")` |
| 2026-06-06 | `perform_save()` closure extracted for both mouse + keyboard save | Save button `on:click` and Ctrl+S handler need identical logic. Shared closure avoids duplication and ensures consistent behavior | Duplicate save logic in two closures |
| 2026-06-06 | Screenshots committed as binary artifacts to `docs/screenshots/` | 4 PNG screenshots at 1264×761, ~500 KB each. Regenerate with synthetic content before next public release. | External image hosting, SVGs |
| 2026-06-06 | Win32 API (ctypes + BitBlt) for screenshot capture over pyautogui | pyautogui failed to activate off-screen window; BitBlt captures client area directly without mouse/cursor pollution | pyautogui .screenshot(region=) |
| 2026-06-06 | DB seeding via Python sqlite3 for demo content | Python 3.12 available; sqlite3 built-in. Seeded 3 scripts + settings without modifying Rust code. DB reset after capture | Rust helper binary, manual UI typing |
| 2026-06-06 | Version bumped 0.1.0 → 0.6.0 for beta release | Phase numbering is 0-based; v0.6.0-beta.1 tag should match source version. Cleaner for users than shipping 0.1.0 as beta | Keeping 0.1.0 (would misrepresent maturity) |
| 2026-06-06 | `softprops/action-gh-release@v2` with `contents: write` permission | GITHUB_TOKEN default permissions don't include release creation. Explicit `contents: write` required for upload | PAT, GHA deploy key |
| 2026-06-06 | Skip code signing for Phase 6 | Code signing certificates cost money and add setup complexity. Unsigned beta is acceptable for early releases; users can click through SmartScreen | Azure Key Vault signing, self-signed cert |
| 2026-06-06 | Skip macOS/Linux builds in Phase 6 | Windows is the primary target; macOS needs Apple developer account/certificates, Linux needs testing on both .AppImage and .deb. Scoped to Windows for MVP | macOS build job, Linux build job |
| 2026-06-06 | `cargo install tauri-cli --version "^2"` over `tauri-action` | Simpler, no third-party action dependency. Compiles from source (slow on first CI run but cached by Swatinem/rust-cache) | `tauri-apps/tauri-action` GitHub Action, `cargo binstall tauri-cli` |
| 2026-06-06 | Dual installer output (MSI + NSIS) via `"targets": "all"` | Tauri v2 defaults to both on Windows. MSI for enterprise deployment, NSIS for lightweight install. No config change needed | Single target (MSI only) |
| 2026-06-06 | Windows portable ZIP via `Compress-Archive` in release.yml | No third-party action needed; EXE + README-INSTALL.txt provides no-install option for users who prefer not to run installers | Pre-built portable ZIP in repo, 7-Zip/NSIS zip2exe |
| 2026-06-06 | `docs/install.md` as separate user-facing doc from `docs/release.md` | `docs/release.md` targets developers (build process, CI, tags); `docs/install.md` targets end-users (download, install, unsigned warnings, safety) | Single doc, README-only |
| 2026-06-07 | Per-platform SHA256 checksum files (not one combined file) | Each CI job runs on a different OS and cannot access other platforms' artifacts. Per-platform files are simpler to implement | Single combined `SHA256SUMS.txt`, `sha256sum --check` with glob |
| 2026-06-07 | Linux/macOS checksum files use build-directory relative paths | `sha256sum` captures path as given; using `--basename` would add complexity. Manual comparison via `Get-FileHash` (Windows) or visual check works without matching directory structure | `cd target/release/bundle/appimage && sha256sum *.AppImage` then adjust output, `sed` stripping |
| 2026-06-07 | NSIS recommended as primary Windows installer over MSI | NSIS handles upgrades and reinstalls reliably. MSI returns 1603 on same-version reinstall (Windows Installer component table inconsistency). MSI suitable for clean first-time deployments only | MSI as primary, NSIS removal (would lose enterprise deployment path) |
| 2026-06-07 | MSI 1603 documented as expected behavior (not a bug) | Same-version reinstall triggers Windows Installer upgrade path issue. Tauri generates unique ProductCode per build but UpgradeCode derived from identifier. First-time MSI install always works. Neither Tauri nor its dependencies can fix this without version increment | Fix in Tauri/WiX, custom MSI wrapper, workaround in Tauri |
| 2026-06-07 | Screenshots not refreshed for Phase 7.1 | Dark theme is still the default; no UI changes since Phase 5. Adding light theme screenshots deferred to a future phase | New light + dark screenshot batch |
| 2026-06-07 | Speed preset buttons over keyboard-only speed control | One-click speed changes are more discoverable and faster for recording users. Keyboard 1-5 still works | Keyboard-only speed control |
| 2026-06-07 | Pause markers `[pause:N]`/`[breath]` in script content (not separate UI) | Authors can embed pauses naturally in script text without switching contexts. Parsed at playback start | Separate pause dialog, timeline editor |
| 2026-06-07 | Pause text highlighted in red | Provides clear visual feedback during rehearsal that pause markers are present | Semi-transparent overlay icon |
| 2026-06-07 | Rehearsal mode as a ScriptLibrary action (not prompter toggle) | Dedicated mode with word count and estimated reading time helps script preparation before entering fullscreen playback | Toggle in prompter, separate tab |
| 2026-06-07 | `ScriptPlaybackState` as separate domain entity + SQLite table | Keeps concerns separated; single table for all per-script preferences (scroll, speed, font, mirror). FK ON DELETE CASCADE ensures cleanup on script deletion | Storing state in existing scripts table as JSON, separate preferences file |
| 2026-06-07 | Periodic save via `set_interval` (3s) during playback | Throttles backend calls without blocking animation loop. Save on pause + save on exit provide redundancy | Save on every scroll change, save on requestAnimationFrame tick |
| 2026-06-07 | Save throttled to interval (not animation-frame-driven) | No backend calls during animation frame. `web_sys::window().set_interval_with_callback_and_timeout_and_arguments_0` with `Closure::forget()` matches existing countdown pattern | `setTimeout` chain, rAF-based throttling |
| 2026-06-07 | `#[allow(clippy::too_many_arguments)]` on save_playback_state | 8 fields needed for per-script preferences. CLI-driven Tauri command API cannot use builder pattern without breaking consistency | Builder/wrapper struct for save params |
| 2026-06-07 | Resume dialog on re-entry (user must choose) | Avoids confusing users who want to start fresh. Explicit choice between resume and start-from-beginning | Auto-resume, auto-resume with undo |
| 2026-06-07 | Jump feedback via toast (not scrollbar position update) | Visual "+5s"/"-5s" toast provides immediate spatial awareness of jump distance without cluttering the reading area | Scrollbar flash, progress bar pulse |
| 2026-06-07 | Shift+Arrow for ~20s jump (not user-configurable) | Matches common media player convention. Arrow alone = small (~5s), Shift+Arrow = large (~20s). Simple to implement and document | Configurable jump distance, mouse wheel jump |
| 2026-06-07 | Per-script preferences stored in same `script_playback_state` table | Single SQLite row per script holds scroll position + all prompter prefs. Simpler than separate preference hierarchy | Separate `script_preferences` table, JSON blob in scripts table |
| 2026-06-07 | No version bump for Phase 9 (stayed 0.8.0 until Phase 9.1) | Phase 9 is an internal milestone merged to main but not released. Version bumped only when creating public beta tag | Bump to 0.9.0 at Phase 9 merge |
| 2026-06-07 | Toast state via `provide_context<ToastState>` (global RwSignal) | Available everywhere without prop drilling. Leptos context pattern matches existing AppState/PlaybackState/UiState | Local component state, event bus |
| 2026-06-07 | `AtomicU32` global counter for toast IDs | `Rc<Cell<u32>>` prevents `ToastState` from being `Copy`. WASM closures capturing non-Copy values become `FnOnce` (can't box as `Box<dyn Fn()>`). `AtomicU32` is `Copy` on all targets including WASM | Local `Cell<u32>` per clone (ID collisions), uuid per toast |
| 2026-06-07 | Toast positioned bottom-right (not center) | Jump feedback was previously centered bottom — would overlap prompter text. Bottom-right avoids reading line entirely | Bottom-center, top-right, center overlay |
| 2026-06-07 | Toast auto-dismiss at 4s | Long enough to read, short enough to not feel sticky. Dismiss button available for impatient users | 2s (too short), 6s (too long), no dismiss |
| 2026-06-07 | `wasm-bindgen-test` over custom test runner | Standard approach for WASM testing in Leptos ecosystem. Works with `wasm-pack test --headless --chrome`, integrates with CI | custom `#[cfg(target_arch = "wasm32")]` + node runner |
| 2026-06-07 | Inline speed error kept (not migrated to toast) | Custom speed input is a form field — validation error next to the control is better UX than a toast notification | Toast for speed error (would require dismissing, distracts from typing) |
| 2026-06-07 | Jump feedback replaced with toast (not kept as separate pattern) | Toast system provides consistent UX + centralized auto-dismiss. Removed 52 lines of signal/effect/display div code | Keep both patterns (duplicate code, inconsistent) |
| 2026-06-07 | Settings panel error display removed (migrated to toast) | Settings errors were inline banners in the settings form. Toast is equally visible and consistent with other components | Keep inline banner for settings (inconsistent) |
| 2026-06-07 | `git filter-repo` over BFG Repo-Cleaner or manual commit removal | Native `--invert-paths --path docs/screenshots/` rewrites 49 commits in one pass. Preserves author/date. Available via `pip install git-filter-repo`. BFG has no path-inversion equivalent | BFG Repo-Cleaner, interactive rebase + filter-branch, manual removal |
| 2026-06-07 | New public repo over re-publishing old repo | Old repo had stale PR refs + workflow runs that GH Support must purge. New repo starts fresh with single commit, no old history/tags/PRs/releases | Re-publish old repo after Support purge (blocked on Support timeline) |
| 2026-06-07 | `robocopy` for clean export over `git clone --depth 1` | Robocopy excludes .git, target, dist, src-tauri/target, databases. No git objects leak. Captures uncommitted or cached files | `git archive`, `git clone --depth 1` + `rm -rf .git` |
| 2026-06-07 | Old Teleprompter repo kept private (not deleted) | Preserves access to old commits (rewritten but still locally accessible in clone). Deletion is irreversible — can delete later after verification | Delete immediately |
| 2026-06-07 | `docs/screenshot-safety.md` checklist added | Prevents recurrence of privacy incident. Mandatory before committing any future screenshots | No checklist (repeat incident risk) |
| 2026-06-07 | Frontend API behind `AppApi` trait + `ApiCtx = Rc<dyn AppApi>` context (Phase 11) | Components testable with a mock without a running Tauri backend. `RealTauriApi` delegates to existing invoke wrappers → production unchanged | Keep direct `tauri_api::*` calls (untestable), generic type param on every component (viral), cfg-swap module |
| 2026-06-07 | `async-trait(?Send)` for `AppApi` | Trait object (`dyn AppApi`) must live in Leptos context; bare `async fn`/RPITIT is not object-safe. `?Send` because frontend is single-threaded WASM | Manual `Pin<Box<dyn Future>>` per method (verbose ×20), enum/function-table dispatch |
| 2026-06-07 | Wrap multi-use handlers in Leptos `Callback` when they capture non-Copy `Rc<dyn AppApi>` | A non-Copy capture moved into a reactive `Fn` closure or used twice becomes `FnOnce` → compile error. `Callback` is `Copy`. Single-use handlers just clone the `Rc` inline | Make api `Copy` (impossible for `Rc<dyn>`), duplicate closures |
| 2026-06-07 | `MockApi` gated `#[cfg(test)]`; component tests inline in `src/component_tests.rs` | Keeps test double out of prod bundle. Inline (not `tests/` crate) so `cfg(test)` is true and MockApi is visible | Always-compiled MockApi (bundle bloat), `tests/` integration crate (can't see cfg(test) items) |
| 2026-06-07 | Mounted-component tests settle async via bounded `tick`/`settle` poll (~6×10 ms) | Deterministic, non-flaky; polls event loop a fixed number of rounds rather than sleeping a wall-clock duration | Fixed `sleep`, dependence on toast 4 s auto-dismiss timing |
| 2026-06-07 | `MockApi::fail_on(cmd)` targeted failure alongside global `failing()` (Phase 12) | Multi-step flows (import = open→read→import) need to fail a *specific* command past earlier successes. Global `failing` would error the first call | Only global failing (can't isolate failure point), per-method bool flags |
| 2026-06-07 | `ConfirmModal` `aria-label="Confirm"/"Cancel"` | Reliable, unambiguous test selection (row "Delete" `title` vs modal "Delete" text collide); also a11y win. Additive markup, no logic change | Select modal button by text (collides with row), by DOM position (brittle) |
| 2026-06-07 | `ToastState::snapshot()` read-only accessor for test assertions | Tests assert toast presence via a cloned `Vec<ToastMessage>`; `get_untracked` avoids reactive subscription | Read `messages` signal directly in tests (couples to internals) |
| 2026-06-07 | `.dv-state.json` phase-close edit kept local-only | Direct push to `main` is policy-blocked (PR-only); dv-state is local pipeline orchestration, not worth a PR per close | PR for one-line state file (overhead), direct main push (blocked) |
| 2026-06-07 | `release.yml` push trigger narrowed to public semver; phase tags no longer fire releases (Phase 13) | Phase tags (`v0.13.0-phase13`) are internal milestones; they previously created junk GitHub Releases via the `v*` trigger. Narrowing avoids accidental public releases | Keep `v*` (junk releases), drop tag trigger entirely (lose real release automation) |
| 2026-06-07 | Pin `windows-2025` + `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` bridge over rewriting actions (Phase 13) | `windows-latest` will silently migrate; explicit pin is reproducible. Node 24 env bridge clears Node 20 deprecation for `rust-cache` without forking it | Wait for breakage, fork rust-cache for Node 24, drop caching |
| 2026-06-07 | No phase tag for Phase 13 | A `v*` phase tag would now fail the narrowed trigger anyway, and the phase is infra-only with nothing to release. Verified via `workflow_dispatch` instead | Create phase tag (no longer triggers release; misleading) |
| 2026-06-07 | Two-step updater commands (`check_for_update` + `install_update`) with `PendingUpdate` managed state (Phase 14) | `tauri-plugin-updater`'s `Update` is not `Serialize` and is consumed by install. Stashing it in `Mutex<Option<Update>>` lets the frontend prompt between detect and install while only seeing serializable `UpdateInfo` | Single check-and-install command (no user prompt), return `Update` to frontend (not serializable), re-check inside install (double network hit) |
| 2026-06-07 | Updater minisign signing is mandatory and distinct from OS code-signing | `tauri-plugin-updater` requires a minisign signature on every artifact (embedded `pubkey` + private signing key). This is separate from the still-out-of-scope Authenticode/notarization. Public key committed; private key git-ignored + GH secret | Treat "no signing" as blocking updater (would kill the feature), ship updater without signature (impossible) |
| 2026-06-07 | Wire updater config now, defer first signed release (Phase 14) | Publishing needs repo secrets + `createUpdaterArtifacts` + a `latest.json` manifest on a real release — out of scope for a code-only, CI-green phase. Config/plumbing land + are documented; `release.yml` passes the secret through harmlessly until enabled | Full publish pipeline now (needs secrets in GH UI + a public release), drop updater (loses the feature) |
| 2026-06-07 | `UpdateBanner` auto-checks on mount; silent when up to date, one toast on check failure; install user-initiated only | Conservative UX: no nag when current, no silent auto-install (trust for unsigned beta), but a failed check is worth surfacing once. Mirrors existing toast + `ApiCtx` patterns | Silent auto-install (bad trust), modal blocking dialog (intrusive), toast on every up-to-date check (nag) |
| 2026-06-07 | macOS builds matrixed over `macos-13` (Intel x86_64) + `macos-14` (aarch64), native per runner (Phase 15) | `macos-latest` is ARM-only → no Intel DMG. Building each arch natively on its own runner avoids cross-compile toolchain/SDK setup and matches the per-arch DMG naming Tauri produces | `cargo tauri build --target x86_64-apple-darwin` cross-compile (SDK/linker setup), lipo universal binary (larger, more complex), drop Intel support |
| 2026-06-07 | Linux RPM via Tauri's `"targets": "all"` (no `rpmbuild`) | Tauri v2 builds rpm with a pure-Rust crate — no system `rpmbuild`/`fpm`. Just collect `bundle/rpm/*.rpm`; zero extra CI deps | Install `rpm`/`fpm` tooling (unneeded), `nfpm` external packager, skip RPM |
| 2026-06-07 | `fail-fast: false` on the macOS matrix | One arch failing should not cancel the other's build/artifacts; both DMGs are independent deliverables | Default `fail-fast: true` (loses the good arch's artifact on the other's failure) |
| 2026-06-07 | Window X hides to tray; Quit (tray) is the only full exit (Phase 16) | Background-running is the point of a tray; matches common desktop apps. One-time toast hint mitigates the "did it close?" confusion | X quits (no background benefit), close = minimize-to-taskbar (no tray), prompt-on-close dialog (nag) |
| 2026-06-07 | Tray logic split into a pure `tray_action(id) -> Option<TrayAction>` seam | The tray/window runtime can't be unit-tested (GUI); extracting the id→action map gives a deterministic, `cargo test`-able core. Shared `MENU_*` constants stop builder/mapper drift | Inline `match` in the menu-event closure (untestable), enum-keyed menu (more boilerplate, still GUI-bound) |
| 2026-06-07 | Tray via `tray-icon` cargo feature on `tauri` (no new crate) | Tray is first-party in Tauri v2 — just a feature flag. Keeps the dependency surface flat | `tauri-plugin-*` for tray (unnecessary), external `tray-icon` crate directly (reinvents tauri integration) |
| 2026-06-07 | One-time hide-to-tray hint via Tauri `event.once` (backend `emit` → frontend toast) | `event.once` auto-unlistens after first fire → hint shows at most once per run with no manual flag. Reuses the existing toast system; no new command/permission | Persisted "shown once ever" flag (storage + plumbing), native OS notification (new plugin), no hint (confusing) |
| 2026-06-08 | Multi-platform updater `latest.json` assembled from per-platform fragment artifacts + a final merge job (Phase 17) | Each platform job signs its own bundle; a fragment (`{signature,url}`) per platform uploaded as a workflow artifact, merged into one `latest.json` by a `needs`-gated job. Asset URLs derived deterministically (GitHub serves spaces as dots) | `tauri-apps/tauri-action` (reverses the cargo-tauri decision, loses portable-zip/checksum steps), single-job build (no per-arch signing), hardcoded URLs (fragile to name normalization) |
| 2026-06-08 | `prerelease` set by tag — `contains(github.ref_name, '-')` (Phase 20) | The updater endpoint `releases/latest/...` only resolves to the latest NON-prerelease; v1.0.0 (hardcoded `prerelease: true`) had to be hand-promoted. Tag-derived flag makes stable releases self-serve | Always-prerelease (breaks updater endpoint), manual promote each release (toil/error) |
| 2026-06-08 | Split macOS into independent arm/intel jobs; manifest depends on win+linux+mac-arm only (Phase 20) | GitHub `macos-13` (Intel) runners are chronically queue-stalled (blocked the v1.0.0 manifest for >70 min, twice). A matrix can't be partially depended on; splitting lets the manifest publish without waiting on Intel. Intel fragment is included when it eventually runs | Matrix + `needs: build-macos` (Intel stall blocks `latest.json`), drop Intel entirely (lose the platform), `if: always()` (still waits on a queued job) |
| 2026-06-08 | Normalize `SHA256SUMS-*` basenames spaces→dots (Phase 20) | GitHub serves asset filenames with spaces replaced by dots; the on-disk space-names in the checksum files broke `sha256sum --check`. Normalizing the basename makes verification work against downloaded assets | Leave space-names (verification fails), rename bundles on disk (riskier, affects signatures/URLs) |
| 2026-06-08 | `app.withGlobalTauri: true` in `tauri.conf.json` (Phase 21 hotfix) | Frontend calls `window.__TAURI__.core.invoke`; Tauri v2 only injects that global when `withGlobalTauri` is true (default false). Without it EVERY command threw → all buttons dead in shipped 1.0.0/1.0.1. | npm `@tauri-apps/api` import (no JS bundler in this Rust/WASM frontend) |
| 2026-06-08 | `#[wasm_bindgen(catch)]` on the `invoke` extern → `Result<JsValue,JsValue>` | A command returning `Err` rejects the JS promise; the non-`Result` binding aborted the spawned task silently (no toast), hiding failures (e.g. the `fileName` bug looked like "nothing happened"). `catch` turns rejections into Rust `Err` that reach the toast | Leave non-catch (silent failures), wrap every call site (verbose) |
| 2026-06-08 | IPC command args MUST be camelCase (`fileName`, `scriptId`, `scrollOffsetPx`) | Tauri v2 converts JS camelCase arg keys → snake_case params. `import_script_from_txt` sent snake `file_name` → "missing required key fileName" → all file imports failed. Single-word args coincidentally matched | snake_case keys (broken for multi-word), `rename_all` on commands (untested, wider blast radius) |
| 2026-06-08 | Dialog commands are `async` + use the non-blocking `pick_file`/`save_file` callback + channel | `blocking_pick_file` deadlocks on the main thread (sync commands run there); an async command blocks the executor. Non-blocking picker (runs on main event loop) + `std::sync::mpsc` recv on the async worker shows the dialog without freezing | `blocking_*` in sync command (main-thread deadlock), `blocking_*` in async command (executor block), spawn_blocking (extra thread) |
| 2026-06-08 | Document import: plain-text extraction via `pdf-extract` (pdf), `zip`+`quick-xml` over `word/document.xml` (docx), light regex strip (markdown) | Teleprompter shows plain text; reduce every format to text. Manual docx zip/xml parse avoids an unmaintained crate. `adapters/document.rs::extract_text` dispatches by extension | `dotext` crate (unmaintained, no pdf), keep markup (reads badly on prompter), office-interop (heavy) |
| 2026-06-08 | Drag-and-drop import handled in the **Rust** `WindowEvent::DragDrop`, not a JS `tauri://drag-drop` listener | The JS listener never fired in practice. The typed backend event imports through `ImportExportService`, then `emit("library-changed")` → frontend refreshes. No JSON-payload guessing | JS `event.listen('tauri://drag-drop')` (didn't fire), HTML5 drop (suppressed when dragDropEnabled) |
| 2026-06-08 | Picture-in-picture = **pin the main window** (`set_pip(enabled)`: small + always-on-top), not a second WebviewWindow | A runtime `WebviewWindowBuilder` window loaded `about:blank` (confirmed over WebView2 CDP: `WebviewUrl::App("index.html")` / `?pip=`/`#pip=` all failed to load the bundled asset). Pinning the already-rendered main window is reliable; same "float over another app" result | Second window + URL param (about:blank), + `initialization_script` global (still about:blank), navigate via CDP (custom protocol not navigable) |
| 2026-06-08 | Only the `main` window hides-to-tray on `CloseRequested` (label guard) | The handler hid every window to the tray, so a secondary window's X couldn't close it. (Moot once PiP became single-window, but correct.) | Hide all windows (PiP couldn't close) |
| 2026-06-08 | Scroll rate extracted to pure `scroll_delta_px(speed,dt)=speed*0.06` (60 px/s per 1×) | The engine used `*0.001` (1 px/s → looked frozen) and disagreed with `estimated_remaining` (assumes `speed*60`). Pure fn keeps them in sync and is unit-testable (`scroll_delta_is_60px_per_second_at_1x`) | Inline magic number (untestable, drifted), tie to font/line height (overcomplex) |
| 2026-06-08 | Prompter `◀◀`/`▶▶` + Up/Down step **0.05** (rounded 2dp) | User wanted fine pacing control; 0.25 was too coarse. Presets still cover big jumps | Keep 0.25, configurable step (overkill) |
| 2026-06-09 | `SettingsRepository::get()` drops the connection lock before seeding defaults | It held the `Mutex<Connection>` guard while calling `self.save()` (locks again) — `std::sync::Mutex` is not re-entrant → first-run settings load deadlocked/froze. Read in a scope, then save | Re-entrant mutex (std has none), `parking_lot` reentrant (new dep), save without lock-drop (deadlock) |
| 2026-06-09 | Resume: reset `scroll_y` on prompter **entry**, save position **synchronously** on exit | Exit zeroed `scroll_y` before the async `save_playback_state` read it → persisted 0 → resume always restarted at top. Capture values sync before save; reset-on-entry still gives a clean start per script. Guarded by WASM test `exiting_prompter_saves_current_scroll_not_zero` | Reset on exit (clobbers save), no reset (next script starts mid-way) |
| 2026-06-09 | Prompter `mousemove` writes `show_controls` only on the hidden→shown transition (`get_untracked` guard) | `RwSignal::set` always notifies; writing `true` on every mousemove re-rendered the controls overlay and recreated the buttons mid-click → "needs two clicks". Guard stops the re-render storm | Dedup signal globally (not available per-set), debounce (timer complexity) |
| 2026-06-09 | OS code signing stays out of scope; ship unsigned (reaffirmed for v1.1.x) | SmartScreen/Gatekeeper warnings are removed only by paid Authenticode/notarization (Azure Trusted Signing ~$120/yr, OV/EV cert, Apple $99/yr). Project is free/unsigned; updater minisign signing is separate and already done | Buy a cert now (cost/decision is the user's), self-signed (doesn't clear SmartScreen) |
| 2026-06-09 | Integration bugs guarded by a manual WebView2-CDP suite (`e2e/cdp/regression.mjs`), not CI | Dead-buttons/import-IPC/PiP/two-clicks are frontend↔backend bugs the headless-WebKit E2E can't run and unit tests can't see. CDP drives the real Windows build (remote-debugging-port). Backend logic + the resume/scroll bugs are covered by in-CI cargo/WASM tests | Rely on tauri-driver E2E (blank page on CI WebKit), no integration coverage (regressions slip) |
